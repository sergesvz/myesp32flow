# -------------------------
# DEEP SLEEP
# Deep sleep uniquement si :
# - pas d’arrosage
# - pas de maintenance
# - pas de flow actif
# pin: GPIO0, GPIO2, GPIO4, GPIO12, GPIO13, GPIO14, GPIO15, GPIO25, GPIO26, GPIO27, GPIO32, GPIO39
# -------------------------

deep_sleep:
  sleep_duration: ${deep_sleep_timer}

status_led:
  pin:
    number: ${deep_sleep_status_pin}
    inverted: true